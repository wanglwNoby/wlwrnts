import * as React from 'react';
import { View, Text } from 'react-native';

const Calls = (): React.ReactElement => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>通话记录页</Text>
        </View>
    );
};

export default Calls;
